# ============================================================
# Core — PDF Parsing
# ============================================================
docling>=2.117.0

# ============================================================
# Chunking
# ============================================================
langchain-text-splitters>=1.1.2
langchain-experimental>=0.4.2
langchain-core>=1.5.3

# ============================================================
# Embedding
# ============================================================
sentence-transformers>=5.6.1
torch>=2.13.0          # GPU: cài bản cu130 từ pytorch.org, không qua pip thường

# ============================================================
# Vector Store — Qdrant local mode (không cần Docker)
# ============================================================
qdrant-client>=1.14.3

# ============================================================
# BM25 + Vietnamese Tokenizer
# ============================================================
rank-bm25>=0.2.2
underthesea>=6.8.4     # Word segmentation tiếng Việt (cần Visual C++ trên Windows)
pyvi>=0.1.1            # Fallback tokenizer nếu underthesea lỗi

# ============================================================
# Reranker (cross-encoder, dùng sentence-transformers CrossEncoder)
# ============================================================
# BAAI/bge-reranker-v2-m3 dùng sentence-transformers — đã có ở trên

# ============================================================
# LLM via Ollama (HTTP client, không cần package ollama)
# ============================================================
requests>=2.32.0       # HTTP client cho Ollama REST API

# ============================================================
# RAGAS Evaluation
# ============================================================
ragas==0.3.9
langchain-ollama>=0.3.0   # LangChain wrapper cho Ollama (dùng trong RAGAS)
datasets>=3.6.0            # HuggingFace datasets (dùng trong RAGAS)

# ============================================================
# Streamlit UI
# ============================================================
streamlit>=1.45.0

# ============================================================
# Validation / Data
# ============================================================
pydantic>=2.13.4
numpy>=2.2.6